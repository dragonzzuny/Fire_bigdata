# 발표·시연 대본 — 불씨예보(K-Firebird)

**10분 발표(시연 3분 포함) + 5분 질의응답**
발표자: 박용준 (아주대학교 산업공학과 석사과정)

듣는 사람은 소방청·소방본부 관계자입니다. 알고리즘 이름보다
**왜 필요한가 / 무엇을 하는가 / 어떻게 쓰는가** 를 먼저 말합니다.

> 대본의 모든 수치는 `outputs/evaluation.json` 실측값입니다.
> 발표 전 "수치 확인" 명령으로 한 번 대조하십시오.

---

## 0. 발표 전 준비 (30분 전)

```bash
cd ~/문서/GitHub/Fire_bigdata

# 1) 대시보드 기동 — 브라우저 탭도 미리 열어 둔다
.venv/bin/streamlit run app/streamlit_app.py

# 2) 수치 확인 — 대본과 다르면 대본을 고친다
.venv/bin/python -c "
import json; ev=json.load(open('outputs/evaluation.json'))
s=json.load(open('outputs/artifacts_summary_ulsan_2021.json'))
h=ev['temporal']['headline']; a=s['allocation']
print('포착', f\"{h['model_capture']:.1%}\", '| 단순기준', f\"{h['baseline_capture']:.1%}\")
print('배분 개선', f\"{a['gain_pp']:+.1f}%p\", '| 상위20%', a['top_k_percent']['n_grids_selected'],
      '구역 지목 → 대상', f\"{a['top_k_percent']['cost_if_all']:,.0f}\", '개소')
"
```

### 체크리스트
- [ ] 브라우저 탭 2개: **대시보드(localhost:8501)** / **발표자료**
- [ ] 사이드바 초기값: 울산 · 2021년 · 점검관 **4명** · 1일 **8건** · **20일**
- [ ] **예방순찰 계획** 탭을 미리 한 번 열어 지도·경로를 캐시에 올려 둘 것
- [ ] **업무 도우미** 탭에서 예시 질문을 한 번 눌러 답변을 받아 둘 것 (첫 호출이 느림)
- [ ] 노트북 절전·알림 끄기

---

## 1. 시간 배분

| 구간 | 시간 | 장표 |
|---|---|---|
| 인사·이름 소개 | 0:20 | 1 |
| 왜 — 문제 제기 | 1:00 | 2 |
| 무엇을 — 시스템 개요 | 0:50 | 3 |
| 활용 데이터 | 0:30 | 4 |
| **시연** | **3:00** | 화면 |
| 어떻게 믿나 — 검증 | 1:20 | 9·10 |
| 기대효과 | 0:40 | 11 |
| 한계 | 0:30 | 12 |
| 마무리 | 0:10 | 13 |
| **합계** | **8:20** | 여유 1분 40초 |

> 장표 5~8번(서비스 화면)은 **시연을 대신하는 장표**입니다.
> 시연이 잘 되면 넘기고, 장비 문제가 생기면 이 장표로 대체합니다.

---

## 2. 대본

### ① 인사 (20초) — 장표 1

> "안녕하십니까. 아주대학교 산업공학과 석사과정 박용준입니다.
> 오늘 발표할 서비스는 **불씨예보**, 영문으로 **K-Firebird** 입니다.
>
> 이름은 두 가지에서 왔습니다. 하나는 애틀랜타 소방의 **Firebird** —
> 머신러닝으로 점검 우선순위를 정해 미국 NFPA 모범사례로 선정된 시스템입니다.
> 그것을 국내 공개 데이터와 소방 법령 체계에 맞춘 한국형이라는 뜻이고,
> 다른 하나는 **일기예보처럼 불씨를 미리 알린다**는 뜻입니다."

---

### ② 왜 (1분) — 장표 2

> "먼저 이 서비스를 왜 만들었는지 말씀드리겠습니다.
>
> 소방 예방행정의 병목은 출동 대응이 아니라 그 앞단, **'어디부터 갈 것인가'** 입니다.
> 소방공무원 증원은 2024년 기준 전년 대비 5명 수준으로 사실상 멈춰 있는데,
> 특정소방대상물과 다중이용업소, 고층건축물은 계속 늘고 있습니다.
>
> 문제는 같은 법정 점검 대상 안에서도 실제 위험이 크게 다르다는 것입니다.
> 용도가 다르고, 업종이 다르고, 과거 화재 이력이 다릅니다.
> 그런데 그 차이를 데이터로 구분해 우선순위를 정하는 체계가 없습니다.
> 지금은 법정 주기와 관할, 담당자 경험에 의존합니다.
>
> (오른쪽을 가리키며) 해외는 이 문제를 데이터로 풀고 있습니다.
> 애틀랜타는 위험점수로 점검 순서를 정하고, 뉴욕 FDNY는 위험기반 점검을 운영합니다.
> 국내 소방 정보화는 출동과 신고 대응 중심이고, **예방점검 우선순위화 영역은 비어 있습니다.**
> 그 자리를 채우려는 것이 이 서비스입니다."

---

### ③ 무엇을 (50초) — 장표 3

> "무엇을 하는 서비스인지 한 장으로 말씀드리면 이렇습니다.
>
> 소방안전 빅데이터로 지역별 화재위험을 예측하고, 그 다음이 중요한데
> **거기서 끝내지 않습니다.** 가용 인력에 맞춰 어디를 갈지 배분하고,
> 119안전센터별 순찰 동선을 만들고, 결재를 올릴 수 있는 계획서까지 만듭니다.
>
> 네 가지 기능입니다. 예방점검 배분, 관서별 순찰, 계획서 자동 생성,
> 그리고 법령을 근거와 함께 찾아 주는 업무 도우미입니다."

---

### ④ 활용 데이터 (30초) — 장표 4

> "사용한 자료는 전부 **소방안전 빅데이터 플랫폼**의 데이터 상품입니다.
> 울산광역시소방본부 4종으로 만들고, 세종특별자치시소방본부 4종으로
> 다른 지역에서도 되는지 확인했습니다.
>
> 여기에 주소를 좌표로 바꾸는 카카오 API, 건조 정도를 계산하는 기상청 자료,
> 그리고 국가법령정보센터의 소방 법령 457개 조문과 법정 서식을 함께 씁니다.
> 개인정보는 다루지 않고 집계 단위 공공데이터만 사용합니다."

---

### ⑤ 시연 (3분)

> **여기서 화면으로 전환합니다.**

#### 시연 1 — 예방점검 배분 (50초)

**[조작]** 대시보드 → **예방점검 배분** 탭

> "실제 울산 데이터로 지금 동작하는 화면입니다.
>
> 왼쪽에 점검 가용 인력을 넣습니다. 점검관 4명, 하루 8건, 20일 — 총 640건입니다.
>
> (상단 지표를 가리키며) 여기가 핵심입니다.
> 위험도 상위 20%를 뽑으면 **292개 구역**이 나오는데,
> 그 구역 안에 있는 점검 대상물이 **26,947개소**입니다.
> 640건으로는 갈 수 없습니다. 위험한 순서대로 돌면 **첫 구역에서 예산이 끝납니다.**
>
> 그래서 인력을 제약으로 넣고 다시 풀었습니다.
> 같은 640건으로 **155개 구역**을 돌 수 있고, 실제 화재를 더 많이 잡습니다."

**[조작]** 사이드바 점검관을 4 → 12로 올린다

> "인원을 늘리면 어떻게 되는지도 바로 보입니다.
> 서장님이 '인력을 더 넣으면 얼마나 좋아지느냐'고 물으실 때
> 이 자리에서 답할 수 있습니다."

**[조작]** 4명으로 되돌린다. 아래 **관할별 배분 형평성** 펼침을 잠깐 보여준다.

> "관할별로 위험 대비 점검이 고르게 갔는지도 함께 확인합니다."

#### 시연 2 — 관서별 순찰 (60초)

**[조작]** **예방순찰 계획** 탭

> "다음은 순찰입니다. 순찰은 구역에서 시작하지 않습니다.
> **119안전센터에서 차를 타고 나가 관할을 돌고 센터로 돌아옵니다.**
>
> (표를 가리키며) 관서별로 몇 개 구역을 도는지, 센터에서 첫 구역까지 몇 km인지,
> 순찰 거리와 복귀 거리, 총 소요시간이 나옵니다.
> **직선거리가 아니라 실제 도로 주행거리**입니다.
>
> (지도) 검은 점이 출동 관서고, 색깔이 관서별 동선입니다."

**[조작]** 순찰 목적을 **일반 예방순찰 → 다중이용업소 야간순찰**로 변경

> "순찰은 목적에 따라 가는 곳이 다릅니다.
> 야간순찰로 바꾸면 유흥주점과 노래연습장이 많은 구역으로 대상이 바뀌고,
> 권장 시간대도 22시에서 새벽 2시로 바뀝니다.
> 확인할 항목도 '영업 중 비상구 폐쇄 여부'처럼 그 시간에만 확인되는 것으로 바뀝니다.
>
> 여섯 가지 순찰 유형이 있고, 화재예방강화지구·피난약자시설·소방용수 점검·
> 건조기 특별경계가 각각 다른 기준으로 대상을 고릅니다."

**[조작]** 아래로 스크롤 → **월별 순찰 강도**

> "'언제'도 함께 봅니다. 최근 8년 화재와 기상을 반영하면
> 12월과 1월이 연평균의 1.1배, 9월이 0.89배입니다.
> 월별 순찰 횟수를 이 값에 맞춰 정합니다."

#### 시연 3 — 계획서 (40초)

**[조작]** **순찰·점검 계획서** 탭 → 기관명 입력 → `계획서 생성`

> "지금까지 만든 것을 결재 문서로 뽑습니다.
> 기관명과 기안자를 넣고 생성하면—"

**[생성 대기 중 멘트]**

> "여기서 중요한 설계 원칙이 있습니다.
> **숫자와 동선, 법령 조문은 시스템이 확정합니다.
> 생성형 AI는 문장만 다듬습니다.**
> 계획의 근거가 AI에서 나오면 검증할 수 없기 때문입니다."

**[결과]**

> "수신·시행일·관련 근거·붙임·결재란까지 공문 서식 그대로입니다.
> 순찰 동선이 순번별로 들어가 있고, 근거 조문이 붙어 있고,
> 조치가 필요할 때 어느 **법정 서식**을 쓰는지도 안내합니다.
> 일별·월별·연간 세 가지를 만들 수 있습니다."

#### 시연 4 — 업무 도우미 (30초)

**[조작]** **업무 도우미** 탭 → 예시 질문 그대로 `질문하기`

> "마지막으로 업무 도우미입니다.
> 소방 법령 7종 457개 조문과 별표, 업종별 점검 항목, 관할 위험 현황을 함께 찾습니다.
>
> (답변) 화재예방강화지구 지정 대상 10개 지역을 조문 번호와 함께 답하고,
> 화재안전조사는 연 1회 이상, 훈련·교육은 10일 전 통보라는 것까지
> 시행령 조문으로 근거를 댑니다.
>
> **'연 1회입니다'만 답하는 시스템은 행정에서 쓸 수 없습니다.**
> 담당자가 조문을 열어 확인할 수 있어야 합니다."

---

### ⑥ 어떻게 믿나 (1분 20초) — 장표 9·10

> "그럼 이 예측을 믿을 수 있는지 말씀드리겠습니다.
>
> 2014년부터 2020년까지 자료로 만들고, **2021년 화재는 만드는 데 한 건도 쓰지 않았습니다.**
> 그 2021년을 맞혀 본 결과입니다.
>
> 위험 상위 20% 구역에 점검을 집중하면 그해 실제 화재의 **68.5%** 가
> 그 안에서 발생했습니다. 아무 데나 갔을 때보다 **3.43배**입니다.
> 신뢰구간까지 붙이면 63.5%에서 72.9% 사이입니다.
>
> (PEI를 가리키며) 그런데 68.5%가 잘한 건지는 이 숫자만으로 알 수 없습니다.
> 화재가 원래 몇 군데에 몰린 지역이면 어떤 방법이든 높게 나오니까요.
> 그래서 **실제 화재를 다 알고 줄 세웠을 때의 값**을 100으로 놓고 비교했습니다.
> 저희는 그 **79%** 수준입니다. 남은 21%가 앞으로 개선할 여지입니다."

**[장표 10]**

> "한 지역에서만 되는 것은 아닌지 네 가지로 확인했습니다.
> 구·군을 하나씩 빼고 학습해도 51에서 61%,
> 울산에서 만든 것을 세종에 그대로 적용해도 74%가 나옵니다.
> 다만 세종은 표본이 작아서 신뢰구간이 넓다는 점을 함께 말씀드립니다."

---

### ⑦ 기대효과 (40초) — 장표 11

> "정리하면, 같은 인력으로 더 많은 화재를 잡습니다.
> 인력에 맞춘 배분은 상위 20% 방식보다 실제 화재 포착이 높고,
> 아무 데나 갔을 때보다 3.43배입니다.
>
> 그리고 소방용수 정책에도 쓸 수 있습니다.
> 울산 전체 구역의 44%에 소화전이 없는데, 그중 **고위험이면서 소화전이 없는 90개 구역**을
> 뽑아냈습니다. 이 목록이 소화전 신설 예산을 요구할 때 쓰는 객관적 근거가 됩니다.
>
> 공개데이터만 쓰기 때문에 별도 운영비가 들지 않습니다."

---

### ⑧ 한계 (30초) — 장표 12

> "마지막으로 알고 있는 한계를 말씀드리겠습니다.
>
> 공개 데이터에 건물번호와 좌표가 없어서 구역 단위로 봅니다.
> 화재의 63%는 도로명이 없어 읍면동 중심 좌표로 배정되는데,
> 도로명이 있는 건만으로 따로 검증해 보니 성능이 떨어지지 않는다는 것은 확인했습니다.
>
> 그리고 **단순 기준 대비 개선폭이 크지 않다는 점도 먼저 말씀드립니다.**
> 누적 화재만으로도 68.4%가 나옵니다.
> 저희가 보는 이 서비스의 가치는 포착률 몇 %p가 아니라
> **왜 위험한지 설명이 되고, 새 지역으로 옮길 수 있고, 인력 제약 안에서 계획이 나온다는 것**입니다."

---

### ⑨ 마무리 (10초) — 장표 13

> "한정된 인력을 가장 위험한 곳에.
> 울산 1,459개 구역, 화재 11,863건으로 검증했습니다. 감사합니다."

---

## 3. 돌발 상황

| 상황 | 대응 |
|---|---|
| **LLM 생성이 40초 넘게 걸린다** | 기다리지 말고 "미리 만들어 둔 결과를 보시겠습니다" 하고 `outputs/inspection_plans/` 파일을 연다 |
| **지도가 안 뜬다** | "지도 타일은 인터넷을 쓰는데, 같은 내용을 표로도 봅니다" 하고 표로 진행 |
| **대시보드가 죽었다** | 장표 5~8번(서비스 화면)으로 대체. 그 사이 터미널에서 재실행 |
| **화면이 옛 버전 같다** | 앱을 재기동한다(코드를 고친 뒤 재기동하지 않으면 옛 모듈이 남는다) |
| **숫자가 대본과 다르다** | 화면 숫자를 그대로 읽는다. "방금 계산된 값입니다"가 오히려 강점 |
| **시간이 밀린다** | 시연 3·4번을 줄이고 1·2번만. 장표 10번을 넘긴다 |

---

## 4. 예상 질문

**Q1. 상위 20%로 68.5%면, 그냥 작년에 불난 곳 가는 것과 얼마나 다릅니까?**
> 단순 기준이 63.5%이니 5%p 차이입니다. 저희가 이 점을 먼저 측정했습니다.
> **누적 화재 하나만으로도 68.4%가 나옵니다.**
> 그래서 포착률을 성과로 내세우지 않습니다. 값은 세 군데에 있습니다.
> **① 왜 위험한지 설명이 나온다는 것, ② 울산에서 만든 것이 세종에서 74%로 작동한다는 것 —
> 화재 기록이 쌓이지 않은 신규 지역에도 쓸 수 있다는 뜻입니다.
> ③ 인력 제약을 넣으면 실제 갈 수 있는 계획이 나온다는 것.**

**Q2. 500m 구역이면 너무 넓지 않습니까? 건물 단위로 안 됩니까?**
> 공개 데이터에 건물번호와 좌표가 없습니다. 화재 자료의 도로명 기재율이 35%이고
> 읍면동은 100% 채워져 있습니다. 그래서 도로명이 있으면 도로 단위로,
> 없으면 읍면동으로 배정하고 **어느 단계로 좌표를 얻었는지 건별로 기록**합니다.
> 부산처럼 상세주소가 포함된 자료를 받으면 건물 단위로 내려갈 수 있습니다.

**Q3. 읍면동 중심으로 뭉치면 성능이 부풀려지는 것 아닙니까?**
> 저희도 의심해서 측정했습니다. **도로명이 확보된 건만으로 다시 검증하니 70.8%로
> 오히려 더 높았습니다.** 부풀림은 확인되지 않았습니다.

**Q4. 2021년 자료가 안 들어갔다는 걸 어떻게 보장합니까?**
> 코드로 강제합니다. 연도 t의 정보는 t-1까지만 쓰도록 만들고,
> **자료를 만들 때마다 실제로 검산해서 어긋나면 멈춥니다.**
> 구역 좌표도 학습에서 뺐습니다. 좌표를 넣으면 '위험한 동네의 위치'를 외워서
> 새 지역에서 무너지기 때문입니다.

**Q5. 형평성은 어떻게 담보합니까?**
> 주장하지 않고 측정했습니다. 관할별로 화재 비중 대비 점검 배분을 계산했더니
> **북구가 0.68로 적게 배정**되고 있었습니다.
> 그래서 배분 단계에 관할별 최소 배분을 제약으로 넣었고,
> 넣어도 포착률이 떨어지지 않는다는 것을 확인했습니다. 화면의 슬라이더로 조절합니다.

**Q6. 생성형 AI가 잘못된 내용을 만들면 어떻게 합니까?**
> 역할을 나눴습니다. **점검 항목은 업종별 규칙이, 위험 판단은 예측이,
> 법령 조문은 원문이 정합니다. AI는 문장만 다듬습니다.**
> 프롬프트에 "표의 숫자, 구역 번호, 관서명, 법령 조문을 절대 바꾸지 말라"를 명시하고,
> 다듬은 결과가 원문보다 크게 짧아지면 채택하지 않습니다.

**Q7. 세종에서 74%면 울산보다 높은데, 이상하지 않습니까?**
> 좋은 지적입니다. **세종은 2021년 화재가 104건뿐이라 표본이 작습니다.**
> 신뢰구간이 64%에서 82%로 넓습니다. 그래서 이 수치는
> "신규 지역에서도 작동한다"는 방향의 근거로만 씁니다.

**Q8. 실제 도입하면 비용이 얼마나 듭니까?**
> 자료는 전부 무료 공개데이터이고, 주소 변환은 무료 한도 안에서 처리됩니다.
> 문서 생성은 기관 내부 서버에서도 돌릴 수 있습니다.

**Q9. 점검 결과가 다시 반영됩니까?**
> 현재는 안 됩니다. 화재안전특별조사 자료에 주소가 없고 대상물 관리번호만 있는데,
> 대상물 자료에는 그 번호가 없어 **결합이 불가능**했습니다.
> 관리번호가 포함된 자료를 받으면 점검 결과 피드백이 가능합니다.

**Q10. 저희가 직접 재현할 수 있습니까?**
> 예. 전 과정 스크립트와 검증 절차를 공개했습니다.
> 자료를 같은 위치에 두고 `./scripts/run_all.sh` 를 실행하면
> **오늘 보신 모든 수치와 이 발표자료가 그대로 다시 만들어집니다.**
